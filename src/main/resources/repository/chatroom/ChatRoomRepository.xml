<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youwin.repository.ChatRoomRepository">

    <select id="findMyRooms"
            parameterType="int"
            resultType="com.youwin.dto.ChatRoomDto">

    SELECT
        cr.room_id,
        cr.room_name,
        cr.target_id,
        cr.theme_id,
        cr.created_at
    FROM chat_rooms cr
    INNER JOIN chat_room_members crm
    ON cr.room_id = crm.room_id
    WHERE crm.member_id = #{memberId}
    ORDER BY cr.created_at DESC
    </select>
</mapper>