<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youwin.repository.MemberRepository">

    <insert id="save" parameterType="com.youwin.dto.MemberDto">
        INSERT INTO member (
            member_id,
            member_password,
            nickname,
            member_phone,
            member_email
        ) VALUES (
            #{memberId},
            #{memberPassword},
            #{nickname},
            #{memberPhone},
            #{memberEmail}
        )
    </insert>

    <select id="countById" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE member_id = #{memberId}
    </select>

</mapper>
