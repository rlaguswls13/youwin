<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youwin.repository.NoticeRepository">

    <!-- 1. 공지사항 작성 (등록) 구역 -->
    <!-- 공지사항 등록 -->
    <insert id="save" parameterType="com.youwin.dto.NoticeDto">
        <!--
          [팁] 글이 작성되는 순간 바로 생성된 notice_id 값을 가져와서
          noticeDto에 자동으로 세팅해 주는 selectKey 구문입니다.
          이것이 있어야 이어서 이미지 파일을 넣을 때 부모 ID인 noticeId를 정확히 인식합니다.
        -->
        <selectKey keyProperty="noticeId" resultType="long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO notice_board (
        member_id,
        category,
        title,
        content,
        is_pinned
        ) VALUES (
        #{memberId},
        #{category},
        #{title},
        #{content},
        #{isPinned}
        )
    </insert>

    <!-- 2. 공지사항 페이지네이션 및 검색 구역 -->
    <!-- 공지사항 전체 조회 -->
    <select id="findAll" resultType="com.youwin.dto.NoticeDto">
        SELECT notice_id AS noticeId,
               member_id AS memberId,
               category,
               title,
               content,
               `count`,
               is_pinned AS isPinned,
               create_at AS createAt,
               update_at AS updateAt
        FROM notice_board
        ORDER BY is_pinned DESC, notice_id DESC
    </select>

    <!-- 페이징 처리 및 검색 조건이 결합된 공지사항 목록 조회 쿼리 -->
    <select id="findAllWithPaging" parameterType="map" resultType="com.youwin.dto.NoticeDto">
        SELECT notice_id AS noticeId,
        member_id AS memberId,
        category,
        title,
        content,
        `count`,
        is_pinned AS isPinned,
        create_at AS createAt,
        update_at AS updateAt
        FROM notice_board
        <where>
            <!-- 카테고리가 'all'이 아닐 때만 해당 카테고리로 필터링 -->
            <if test="category != null and category != 'all'">
                AND category = #{category}
            </if>
            <!-- 검색 키워드가 존재할 때 제목 또는 작성자 ID에서 포함 여부 검색 -->
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%') OR member_id LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        <!-- 고정글이 항상 최상단에 오고, 그 뒤로 최신글 순 정렬 -->
        ORDER BY is_pinned DESC, notice_id DESC
        <!-- 10개씩 끊어오기 위한 페이징 구문 추가 -->
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 페이지네이션 하단 연산을 위해 조건에 맞는 게시글 총 개수를 구하는 쿼리 -->
    <select id="countTotal" parameterType="map" resultType="int">
        SELECT COUNT(notice_id)
        FROM notice_board
        <where>
            <if test="category != null and category != 'all'">
                AND category = #{category}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%') OR member_id LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <!-- 3. 공지사항 삭제 구역 -->
    <!-- 공지사항 삭제 SQL 쿼리 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM notice_board
        WHERE notice_id = #{noticeId}
    </delete>

    <!-- 4. 공지사항 수정 구역 -->
    <!-- 공지사항 데이터 수정 UPDATE 쿼리 -->
    <update id="update" parameterType="com.youwin.dto.NoticeDto">
        UPDATE notice_board
        SET category = #{category},
            title = #{title},
            content = #{content},
            is_pinned = #{isPinned},
            update_at = CURRENT_TIMESTAMP
        WHERE notice_id = #{noticeId}
    </update>

    <!-- 5. 공지사항 단건 상세 조회 구역 -->
    <!-- 공지사항 단건 상세 조회 (더블클릭 조회 연동) -->
    <select id="findById" parameterType="long" resultType="com.youwin.dto.NoticeDto">
        SELECT notice_id AS noticeId,
               member_id AS memberId,
               category,
               title,
               content,
               `count`,
               is_pinned AS isPinned,
               create_at AS createAt,
               update_at AS updateAt
        FROM notice_board
        WHERE notice_id = #{noticeId}
    </select>

    <!-- 6. 공지사항 이미지 처리 구역 -->
    <!-- 업로드된 이미지 정보를 첨부파일 테이블에 기록하는 쿼리 -->
    <insert id="insertImage" parameterType="com.youwin.dto.NoticeImageDto">
        INSERT INTO notice_images (
            notice_id,
            original_name,
            saved_file_name,
            reg_date
        ) VALUES (
                     #{noticeId},
                     #{originalName},
                     #{savedFileName},
                     CURRENT_TIMESTAMP
                 )
    </insert>

    <!-- 상세조회 시 특정 공지사항 ID에 묶인 이미지 목록을 불러오는 쿼리 (image_id 매핑 추가) -->
    <select id="selectImagesByNoticeId" parameterType="long" resultType="com.youwin.dto.NoticeImageDto">
        SELECT image_id AS imageId,
               notice_id AS noticeId,
               original_name AS originalName,
               saved_file_name AS savedFileName,
               reg_date AS regDate
        FROM notice_images
        WHERE notice_id = #{noticeId}
        ORDER BY reg_date ASC
    </select>

    <!-- 공지사항 글 삭제 시 연결되어 있던 모든 이미지 데이터를 일괄 제거하는 쿼리 -->
    <delete id="deleteImagesByNoticeId" parameterType="long">
        DELETE FROM notice_images
        WHERE notice_id = #{noticeId}
    </delete>

    <!-- [추가] 공지사항 수정 시 유지되지 않은 개별 이미지를 DB에서 삭제하는 쿼리 -->
    <delete id="deleteImageById" parameterType="long">
        DELETE FROM notice_images
        WHERE image_id = #{imageId}
    </delete>

</mapper>